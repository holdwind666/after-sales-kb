name: validate

on:
  push:
  pull_request:

jobs:
  windows-smoke:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run fresh-install smoke test
        shell: powershell
        run: .\tests\smoke.ps1
